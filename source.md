# Robot Manipulation 연구 위키 — VLA / RFM / Trajectory Optimization / MPC

최종 갱신: 2026-09-03

---

## 0. 문서 범위와 작성 규칙

이 문서는 로봇 매니퓰레이션 분야의 네 계열(VLA, 로봇 파운데이션 모델·월드모델, 궤적 최적화, 모델예측제어)에 대해 2023년부터 2026년 8월까지의 주요 논문을 계보 중심으로 정리하고, 각 논문이 스스로 명시한 한계와 벤치마크 논문이 실측으로 드러낸 실패 지점을 근거로 미해결 영역을 식별한 것이다.

작성 규칙은 세 가지다. 첫째, 성능 수치와 주장은 원 논문 또는 공식 릴리스가 보고한 값을 그대로 옮기며, 서로 다른 논문의 수치는 평가 프로토콜이 다르므로 직접 비교하지 않는다. 둘째, "미해결"이라는 판정은 서베이·벤치마크·해당 논문의 limitation 절이 명시적으로 진술한 내용에만 근거하며, 정리자의 추정이 들어간 부분은 별도로 표시한다. 셋째, 2026년 arXiv 식별자는 YYMM 규칙을 따르므로 2601은 2026년 1월, 2608은 2026년 8월을 의미한다.

주의할 점 하나. 산업계 릴리스(Gemini Robotics, GR00T)의 성능 주장은 상당수가 논문이 아니라 블로그·보도자료 기반이고 독립 재현이 없다. 이 문서에서는 출처 유형을 구분해 표기했다.

---

## 1. 전체 지형도

네 계열은 경쟁 관계가 아니라 서로 다른 시간 스케일과 다른 정보를 다룬다. 대략적으로 정리하면 이렇다.

VLA는 언어·시각으로부터 수 초 단위의 action chunk를 생성하는 정책이며, 일반화의 원천은 사전학습된 VLM의 의미 지식이다. RFM/월드모델은 여기에 물리 동역학 예측을 얹어, 데모 데이터가 없는 동작·환경으로의 전이를 노린다. 궤적 최적화는 충돌·기구학·동역학 제약을 명시적으로 만족하는 경로를 초 이하 시간에 푸는 계층이고, MPC는 그 최적화를 매 제어 주기마다 다시 풀어 접촉과 외란에 반응한다.

2026년 시점에서 실제 시스템은 대부분 혼합 구조다. π 계열은 VLM 기반 상위 계층이 이산 토큰으로 서브골을 뱉고 flow matching action expert가 연속 제어를 담당하는 이중 구조이며, cuRobo·CI-MPC 계열은 산업 배치에서 학습 정책의 출력을 안전하게 실행 가능한 궤적으로 사영하는 역할을 맡는다. 그런데 이 두 세계를 잇는 인터페이스는 아직 표준이 없다. 이 점이 8장에서 다루는 핵심 공백이다.

---

## 2. VLA (Vision-Language-Action)

### 2.1 계보

**RT-2 (2023, Google DeepMind).** VLM을 로봇 데모로 파인튜닝해 이산화된 액션 토큰을 출력하게 만든 최초 사례로, VLA 패러다임 자체를 정립했다. PaLI-X와 PaLM-E를 백본으로 사용했다.

**OpenVLA (CoRL 2024).** 오픈소스 generalist 정책. 액션 토큰을 LLaMA 토크나이저의 저빈도 토큰 256개 자리에 직접 끼워 넣어 next-token prediction으로 다룬다. 이후 대부분의 오픈소스 비교 실험의 기준선이 되었다. 후속 OpenVLA-OFT(2502.19645)는 파인튜닝 레시피를 정비해 속도와 성공률을 함께 올렸다.

**π0 (2410.24164, Physical Intelligence).** 약 3B PaliGemma 백본에 300M 규모의 flow matching action expert를 붙인 구조. 이산 토큰의 양자화 오차 없이 연속 action chunk를 생성한다는 점이 핵심이며, 세탁물 접기·박스 조립 같은 양팔 과제에서 당시 최고 성능을 보고했다.

**π0.5 (2504.16054).** 실제 가정 환경 배치를 목표로 한 버전. 상위 계층이 FAST 토크나이저로 학습된 이산 토큰을 내놓고, 그것을 조건으로 flow matching 하위 계층이 동작을 만드는 계층 구조를 명시적으로 채택했다.

**π*0.6 / RECAP (2511.14759).** 모방학습의 근본 문제인 오차 누적을 실배치 RL로 다룬다. RECAP은 advantage conditioning을 이용해 데모, 자율 수집 데이터, 전문가의 실시간 개입 교정을 하나의 학습 신호로 통합한다. 오프라인 RL로 generalist를 사전학습한 뒤 다운스트림 과제에 온로봇 데이터로 특화시키는 2단계 구조다. 논문은 어려운 과제에서 처리량 2배 이상, 실패율 2배 이상 감소를 보고하며, 에스프레소 제조·박스 조립·다양한 세탁물 접기를 실증 과제로 제시한다.

**π0.7 (2604.15483, 2026년 4월).** 현재 π 계열 최신. 핵심 아이디어는 학습 시 다양한 컨텍스트 조건화(diverse context conditioning)로, 무엇을 할지뿐 아니라 어떤 방식·전략으로 할지를 프롬프트에 담아 모델을 정밀하게 조종한다는 것이다. 서브골 이미지를 포함한 멀티모달 프롬프트를 사용하며, 학습 데이터에 데모·실패를 포함한 준최적 자율 데이터·비로봇 데이터를 섞는다. 논문은 미지 환경에서의 언어 지시 수행, zero-shot cross-embodiment 일반화(학습에서 본 적 없는 로봇으로 세탁물 접기), 그리고 에스프레소 머신 조작에서 RL로 특화된 전문 모델과 대등한 out-of-the-box 성능을 주장한다. 학습 레시피는 knowledge insulation을 사용한다.

**Gemini Robotics 계열 (Google DeepMind).** 2025년 3월 Gemini Robotics / Gemini Robotics-ER 최초 공개, 6월 On-Device 변형, 이후 1.5를 거쳐 2026년 7월 30일 Gemini Robotics 2 발표. GR2는 세 모델 묶음이다. VLA 본체(전신 제어 포함), 임베디드 추론용 VLM인 ER 2(수 분 길이의 다단계 계획, 다중 로봇 협업), 그리고 온디바이스 버전(수 시간 분량 데이터로 새 embodiment에 빠르게 적응). Apptronik Apollo 2, SharpaWave 다지 손, Inspire 손, Franka Duo + Robotiq 그리퍼를 동일 체크포인트로 구동한 결과를 공개했다. **여기서 주목할 것은 DeepMind 자신의 보고 내용이다.** 전신 과제와 그리퍼 기반 dexterous 과제에서는 중간~높은 성공률을 얻었지만, 다지 손 기반 정밀 조작(multi-finger dexterous manipulation)은 여전히 어렵다고 명시했다. 다만 이 모델들은 신뢰 테스터에게만 제공되며 상용 구매가 불가능하고, 논문 형태의 상세 평가는 공개되지 않았다.

**NVIDIA Isaac GR00T 계열.** N1(2025, 최초 오픈 휴머노이드 파운데이션 모델) → N1.5 → N1.7(상용 라이선스, 조기 접근) → N2. N2는 뒤의 DreamZero 연구에 기반한 world action model 아키텍처로, 새 환경의 새 과제 성공률이 기존 주요 VLA 대비 2배 이상이라고 발표했으며 MolmoSpaces와 RoboArena에서 generalist 정책 1위라고 주장한다. 연말 공개 예정으로 예고된 상태다(GTC 발표 기준).

**중국계 오픈 생태계.** RDT-1B(2410.07864)는 양팔 조작용 확산 파운데이션 모델, AgiBot World Colosseo(2503.06669)는 100대 로봇으로 수집한 100만+ 궤적 데이터셋과 latent action planner 기반 generalist 정책 GO-1을 함께 공개했다. GO-1은 OXE 사전학습 대비 in-domain 0.47→0.77, OOD 0.38→0.67의 완료 점수 향상을 보고했다. 2026년에는 RDT2(UMI 데이터의 스케일 한계와 zero-shot cross-embodiment), Being-H0.5/H0.7, DM0, GigaBrain-0.5M 등이 이어졌다.

### 2.2 액션 표현이라는 축

VLA 논문 대부분의 실질적 차이는 액션을 어떻게 표현하느냐로 환원된다. 이 축의 트레이드오프는 AAAI 2026의 open challenges 논문(2511.05936)이 비교적 명료하게 정리했다.

이산 표현은 각 축을 분위수 기반 256 bin 정도로 나누고 transformer의 next-token prediction을 그대로 쓴다. 구현이 단순하지만 양자화 오차가 있고, 자기회귀 디코딩 때문에 3~5Hz 수준의 추론 속도에 머물러 고주파 제어와 양팔 조작에는 부적합하다는 것이 여러 논문의 공통 진술이다. FAST(2501.09747)는 액션 청크에 이산 코사인 변환을 적용해 차원 간 상관을 제거한 뒤 BPE로 압축하는 방식으로 이 문제를 크게 완화했다. 한 서베이의 계산에 따르면 50스텝 × 16차원의 청크가 16~32 토큰으로 줄어든다.

연속 표현은 확산 또는 flow matching action expert가 청크 전체를 한꺼번에 생성한다. 다봉 분포를 잘 다루고 고주파 제어에 적합하지만 수렴에 훨씬 큰 연산 예산이 필요하고, 확산 기반은 50~100 스텝 디노이징으로 flow matching 대비 2~4배 지연이 발생한다는 보고가 있다.

**Knowledge insulation (2505.23705)** 은 이 두 가지를 결합할 때 생기는 부작용을 다룬 논문으로, π0.7까지 이어지는 표준 레시피가 되었다. 초기화되지 않은 action expert의 그래디언트가 VLM 백본으로 역전파되면 사전학습된 의미 지식이 손상된다는 것을 실험적으로 보이고, 백본은 이산 액션 토큰으로 파인튜닝하되 action expert의 그래디언트는 차단하는 구조를 제안한다.

추론 지연을 줄이려는 계열도 두텁다. 일관성 증류 계열(ManiFlow, MP1, SnapFlow 2604.05656)은 1스텝 생성을 목표로 하고, 병렬 디코딩·조기 종료(CEED-VLA), 동적 추론(DeeR-VLA), 서브 4비트 양자화(ActQuant) 등이 각각 다른 지점을 공략한다.

### 2.3 사후학습 RL

2026년 VLA 연구의 가장 활발한 축이다. RECAP 외에 π_RL(2510.25889, flow 기반 VLA 온라인 RL 파인튜닝), ReinFlow(2505.22094), ConRFT(2502.05450, consistency policy 기반), 잠재공간 RL로 확산 정책을 조향하는 접근(2506.15799), Z-1(2606.31846, 효율적 VLA RL) 등이 있다. π*0.6 논문 자신이 남긴 진술이 이 분야의 현재 상태를 잘 요약한다. VLA를 실사용 가능한 신뢰도까지 끌어올리는 가장 직접적인 경로는 RL로 보이지만, 대용량 모델의 대규모 RL 학습 안정성, 표본 복잡도, 자율성, 지연된 피드백 등이 모두 미해결이며 LLM용 RL 프레임워크는 출발점 정도만 제공한다는 것이다.

### 2.4 VLA 주요 논문 색인

| 논문 | 식별자 | 핵심 기여 |
|---|---|---|
| RT-2 | 2307.15818 | VLM 파인튜닝으로 액션 토큰 출력, VLA 패러다임 정립 |
| OpenVLA | CoRL 2024 | 오픈소스 generalist, LLaMA 어휘에 액션 토큰 삽입 |
| Octo | RSS 2024 | 80만 데모 학습 경량 generalist, 확산 헤드 |
| π0 | 2410.24164 | flow matching action expert, 양팔 SOTA |
| FAST | 2501.09747 | DCT+BPE 액션 토크나이저, 자기회귀 VLA 속도 개선 |
| π0.5 | 2504.16054 | 이산 상위계층 + 연속 하위계층, 실가정 배치 |
| Knowledge Insulation | 2505.23705 | action expert 그래디언트 차단, 지식 보존 |
| OpenVLA-OFT | 2502.19645 | 파인튜닝 최적화 (속도·성공률) |
| π*0.6 / RECAP | 2511.14759 | advantage conditioning 기반 실배치 RL |
| π0.7 | 2604.15483 | 다양한 컨텍스트 조건화, zero-shot cross-embodiment |
| Gemini Robotics 2 | 릴리스(2026.07) | 전신 제어, 다중 embodiment 단일 체크포인트 |
| GR00T N1 / N2 | 2503.14734 / 릴리스 | 오픈 휴머노이드 FM / world action model 기반 |
| GO-1 (AgiBot) | 2503.06669 | latent action planner, 100만 궤적 데이터셋 동반 |
| SafeVLA | 2503.03480 | CMDP 기반 안전 정렬, 위반 비용 83.58% 감소 보고 |

---

## 3. RFM · World Model 계열

### 3.1 World Action Model의 등장

2026년의 가장 큰 방향 전환은 "VLA는 의미 일반화는 잘하지만 물리 동작 일반화는 못한다"는 진단이 정량화되면서 일어났다.

**DreamZero (2602.15922, NVIDIA GEAR).** 14B 규모 world action model로, Wan2.1-I2V-14B 이미지-투-비디오 확산 백본 위에 구축했다. 미래 영상 상태와 액션을 동시에 예측하며, 영상을 세계 변화의 밀집 표현으로 사용한다. 보고 수치는 미지 과제 평균 진행률 39.5%, 일반화 2배 이상 개선, 그리고 다른 로봇이나 사람의 영상만으로 10~20분 분량 데이터를 주었을 때 42% 이상의 상대 개선이다. 30분(55 궤적) 분량의 play 데이터로 새 양팔 로봇(YAM)에 적응하면서 zero-shot 일반화를 유지했다는 few-shot embodiment adaptation 결과도 함께 보고했다.

지연 문제는 이 계열의 최대 난점인데, DreamZero는 비동기 폐루프 실행, CFG 병렬화, DiT 캐싱, Torch Compile/CUDA Graphs, NVFP4 사후 양자화, cuDNN 어텐션 백엔드를 합쳐 5.7초를 150ms로 38배 단축해 7Hz 폐루프 제어를 달성했다고 기술한다. 7Hz라는 숫자 자체는 정밀 접촉 과제 기준으로는 낮은 편이라는 점을 함께 봐야 한다.

NVIDIA의 기술 블로그는 이 계열의 비용 구조도 공개했는데, DreamZero식 액션 튜닝이 약 9 ZFLOPs, 전체 비디오 사전학습 + 적응은 50 ZFLOPs를 넘길 수 있다고 한다. Fast-WAM은 추론 시 영상 생성을 건너뛰는 표현 전용 접근으로 이 비용을 피하려 한다.

**latent action 계열.** 액션 라벨이 없는 영상에서 잠재 액션을 추출해 정책 학습에 쓰는 방향이다. LAPA(ICLR 2025), GO-1의 latent action planner, CoMo(2505.17006), Being-H0.7(2605.00078) 등이 여기 속한다. V-JEPA 2(2506.09985)는 픽셀 재구성 대신 잠재 임베딩 예측으로 장기 예측 효율을 확보하는 대안 노선이다.

### 3.2 데이터와 스케일링 법칙

이 부분이 2026년에 가장 명확한 정량 근거가 쌓인 영역이다.

**EgoScale (2602.16710).** 20,854시간의 액션 라벨링된 1인칭 인간 영상으로 flow 기반 VLA를 사전학습했다. 선행 연구 대비 20배 이상 규모이며, 인간 데이터 규모와 검증 손실 사이의 로그-선형 스케일링 법칙을 발견하고 이 손실이 실로봇 성능과 강하게 상관한다는 것을 보였다. 대규모 인간 사전학습 → 인간-로봇 정렬 미드트레이닝의 2단계 레시피로 22-DoF 다지 손에서 무사전학습 대비 평균 성공률 54% 개선을 보고했다.

**HumanScale (2606.20521).** 더 도발적인 결과다. 동일한 사전학습 데이터량 조건에서 1인칭 인간 영상으로 사전학습한 모델이 원격조작 실로봇 궤적 사전학습보다 실로봇 액션 예측 검증 손실 24% 낮고, in-distribution 성공률 52.5%, OOD 성공률 90% 높았다고 보고한다. 100~5000시간 구간에서 로그-선형 법칙($\mathcal{L} = a - b \ln D$, seen $R^2=0.86$, unseen $R^2=0.94$)이 성립하고 아직 포화되지 않았다는 것이 논지다. 저자들은 이 우위의 원인을 데이터 다양성으로 귀속시킨다.

다만 이 결과들을 읽을 때 함께 볼 반론이 있다. 업계 정리 글들은 "인터넷 스케일"이라는 표현이 실제 비용을 과소평가한다고 지적한다. 손이 프레임에 잘 들어오고 카메라 흔들림이 적은 고가치 영상은 계측 장비(헤드셋, UMI 그리퍼)와 큐레이션을 요구하기 때문이다.

### 3.3 cross-embodiment

**UniAct(CVPR 2025)** 는 코드북 기반 범용 원자 액션을 학습한 뒤 로봇별 디코더로 풀어내는 방식으로, 새 로봇 적응에 필요한 데이터를 크게 줄였지만 진정한 zero-shot에는 못 미친다고 스스로 평가한다. 2026년의 RDT2, MOTIF(action motif 기반 few-shot cross-embodiment), Cross-Hand Latent Representation(2603.10158)이 같은 문제를 각각 다른 각도로 다룬다.

---

## 4. Trajectory Optimization

### 4.1 GPU 병렬화 노선

**cuRobo (2310.17274).** L-BFGS 스텝 방향 추정 + 병렬 잡음 라인서치 + 입자 기반 최적화를 결합하고, 다수의 시드를 동시에 최적화한다. 어려운 문제를 평균 50ms 내에 해결하며 기존 궤적 최적화 대비 60배 빠르다고 보고했다. 병렬 기하 플래너 20ms, 충돌 없는 IK 초당 7000~9000 질의 이상.

**cuRoboV2 (2603.05493, 2026).** 세 가지를 바꿨다. B-스플라인 궤적 파라미터화로 평활성과 토크 한계를 강제하고, GPU 네이티브 TSDF/ESDF 인식 파이프라인으로 작업공간 전체를 덮는 조밀 거리장을 생성하며(기존 대비 최대 10배), 고 DoF 시스템을 위한 위상 인식 기구학·미분 가능 역동역학·map-reduce 자기충돌 검사를 넣었다. 벤치마크에서 계획 시간 48ms(cuRobo 36ms)로 다소 늘었지만 실행 가능성이 보장되고, 75퍼센타일 에너지 소비 106J로 cuRobo 116J, VAMP 131~160J보다 낮으며 페이로드 문제 2600개 중 2591개를 풀었다(cuRobo+NewtonDyn 2525). 2026년 4월 Apache 2.0으로 공개되었다.

이 계열의 의미는 단순한 속도 개선이 아니다. per-timestep 파라미터화가 만들어내는 불연속적 가속도·저크 프로파일이 실제 실행 불가능성의 원인이었고, 스플라인 표현이 최적화 지형 자체를 더 잘 조건화한다는 것이 cuRoboV2의 주장이다.

**cuTAMP (2411.11833).** GPU 병렬 TAMP. 조합적 샘플링으로 초기 파라미터 입자 집합을 만들고 공동 최적화로 계획 제약을 만족시킨다. 입자 수를 늘릴수록 계획 시간과 해의 품질이 함께 개선된다는 결과가 핵심이며, 장기 매니퓰레이션 문제를 초 단위로 푼다.

### 4.2 접촉을 명시적으로 다루는 최적화

**CITO / MPCC.** 접촉 지점이 닿지 않을 때 접촉력이 0이어야 한다는 상보성 제약을 수학 프로그램에 넣는 정식화다. 문제는 허용 접촉점 수가 늘어나면 해 시간이 가파르게 증가한다는 것이고, **STOCS**는 반복 최적화 과정 안에서 유효 접촉점과 시점을 동적으로 식별해 매 MPCC 호출의 변수·제약 수를 줄이는 방식으로 이를 완화한다.

**Push Anything (2510.19974).** 첫 관측만으로 단일·다물체 밀기를 수행하는 CI-MPC 시스템.

### 4.3 생성모형 기반 궤적 생성

이 축은 "최적화의 초기해를 학습으로 대체한다"는 문제의식에서 출발한다. 최적화 기반 플래너의 성능은 초기해에 크게 좌우되는데, 샘플링 기반 플래너로 얻는 초기해는 고차원 복잡 장면에서 느리고 비평활하다.

**MPD (2412.19948)** 는 확산 모델로 궤적 분포 사전을 학습하고, planning-as-inference 틀에서 비용 유도 사후 샘플링으로 학습된 사전과 과제별 목적을 섞는다. **FlowMP (2503.06135)** 는 conditional flow matching으로 반복 디노이징 없이 소수 스텝에 궤적을 만들고 2차 동역학(가속도 프로파일)을 다룬다. **Model-Based Diffusion (NeurIPS 2024)** 은 확산 모델을 사전 학습 없이 최적화 솔버 자체로 쓴다. **FSD-MP (2607.02977)** 는 궤적을 연속 함수로 보고 함수 공간에서 확산해 이산화 해상도에 불변인 생성을 시도하며, 스펙트럼 영역의 mode-wise forward process와 DST 기반 Fourier Neural Operator로 시작·목표 제약을 해상도에 걸쳐 보존한다.

제약 만족 계열로는 SafeDiffuser(제어 장벽 함수 통합), Diffusion-CCSP(기하·물리 조건 합성), Constraint-Aware Diffusion(하이브리드 손실로 제약 위반 최소화), LTLDoG(시간 논리 제약)가 있다. 공통 한계는 소프트 제약으로 다루는 이상 위반 가능성이 남고, 배치 최적화 후 필터링에 의존한다는 점이다.

---

## 5. Model Predictive Control

### 5.1 Contact-Implicit MPC

이 계열의 문제 정의는 명확하다. 범용 정밀 조작을 하려면 접촉 시퀀스를 실시간으로 고안하고 실행해야 하는데, 가능한 접촉 시퀀스의 수는 지수적이라 기존 모델 기반 제어기는 실시간 전역 최적화가 불가능하다. 그래서 지금까지의 진전은 단순화된(그러나 여전히 하이브리드인) 모델로 국소 근사를 취하는 방향이었고, 그 결과 제어기가 가까운 접촉만 활용할 수 있어 접촉 공간 탐색에 외부 개입이 필요했다.

**Fast CI-MPC (Le Cleac'h et al., T-RO 2024)** 와 **CI-MPC for quadrupeds (Kim et al., IJRR)** 가 실시간성 기반을 닦았고, 보고된 계산 성능은 24-DoF 휴머노이드 50Hz, 양팔 100Hz(Kurtz et al. 2023의 역동역학 궤적 최적화), 평면 밀기 루프당 1초 이하(Bui et al. 2025) 수준이다.

**전역 근사 (2505.13350, Venkatesh et al.).** 국소 상보성 기반 제어와 저차원 전역 샘플링을 결합한다. 핵심 착상은 매 제어 루프마다 접촉 없는 단계가 접촉 풍부 단계에 선행한다고 보고, 접촉 없는 단계가 이동할 수 있는 엔드이펙터 위치들을 병렬 샘플링한 뒤 각 샘플 근방의 접촉 MPC가 예측하는 비용을 비교하는 것이다. 다물체 조작 같은 비볼록 과제에서 국소 최소점을 벗어나는 경로를 제공한다.

**Inverse Dynamics Trajectory Optimization (Kurtz et al. 2023).** 위치만으로 정식화하고 컴플라이언트 접촉 모델과 맞춤 신뢰영역/도그레그 솔버를 써서 차원을 줄인다.

### 5.2 샘플링 기반 MPC

MPPI 계열이 GPU 시뮬레이터와 결합하면서 접촉 문제에 다시 유효해졌다. IsaacGym을 동역학 모델로 쓰는 MPPI, 병렬화 가능한 물리 시뮬레이션을 활용하는 접근(2501, Pezzato et al.), Feedback-MPPI(RA-L 2026, 롤아웃 미분으로 저수준 제어기 없이 빠른 샘플링 MPC) 등이 있다.

**실기 배치 결과 (2606.20712).** 이 논문이 특히 참고할 만한 이유는 실패 분석이 구체적이기 때문이다. JAX 대규모 병렬화 + MuJoCo MJX를 Franka Research 3에 real-to-sim-to-real 파이프라인으로 배치해 Push-T를 수행했다. 구조적 전역 샘플링을 쓰는 Model Tensor Planning 변형이 모드 전환이 필요한 과제에서 CEM·MPPI·PS 같은 단봉 기준선을 시뮬레이션과 실기 양쪽에서 앞섰다. 동시에 온라인 도메인 랜덤화 실험에서, 접촉 개시 파라미터는 해석 가능한 적응 신호를 주지만 전역 물리 파라미터는 통상적 재계획 주파수에서 신뢰성 있게 활용하기엔 피드백이 너무 약하다는 것을 확인했다. 저자들이 정리한 난점 세 가지는 접촉 민감성, 빠듯한 연산 예산, 실시간에서 유의미한 도메인 랜덤화 신호를 얻기 어려움이다.

### 5.3 학습과의 결합

**Where to Touch, How to Contact (2601.10930).** 기하 인식 장기 정밀 조작을 위한 계층적 RL-MPC 구조로, 상위가 접촉 위치를, 하위 MPC가 접촉 방법을 담당한다.

**ProbeAct (2606.09740).** 학습된 정책 위에 안전망을 얹는 접근. VLA 중간 특징에서 물체 3D 위치를 예측하는 경량 프로브, 그리퍼 내부 신호와 엔드이펙터 기구학만으로 파지·이송·배치 실패를 감지하는 물체 무관 상태 기계, 반복 실패 위치를 소프트 안전집합 제약으로 인코딩하는 계층적 CBF 필터로 구성된다. 학습 불필요한 플러그앤플레이이며 LIBERO-plus에서 OpenVLA-OFT를 69.6% → 74.1%로 올렸다.

---

## 6. 평가 인프라의 현황

이 장은 아래 7장의 "미해결" 판정 근거이므로 별도로 둔다.

**시뮬레이션 벤치마크.** LIBERO, SimplerEnv, CALVIN, RLBench가 표준이지만, open challenges 논문(2511.05936)은 이들이 실제 환경의 조명·반사·표면 질감·관절 PD 파라미터를 충분히 재현하지 못해 시뮬레이션 성능과 실기 성능의 상관이 낮다고 지적한다. SimplerEnv는 이미지 인페인팅과 시뮬레이티드 어닐링으로 분포 차이를 줄였으나 여전히 개선 여지가 크다는 것이 같은 논문의 평가다.

**RoboArena (2506.18123, ICLR 2026).** 고정 과제·환경을 표준화하는 대신 분산 평가자 네트워크로 크라우드소싱하고, 이중맹검 비교와 랭킹 체계를 쓴다. DROID 플랫폼 기반이며 DROID로 파인튜닝된 정책들을 대상으로 한다.

**RobotArena∞ (ICLR 2026).** RoboArena가 DROID 환경과 다섯 개 과제·세 개 정적 장면 수준에 머무는 한계를 지적하며 확장성 있는 real-to-sim 평가 틀을 제안한다. 이 논문의 실측 결론이 중요한데, VLA는 데이터셋 차이에 매우 민감해서 학습 분포 밖 환경에서 성능이 떨어지며, 현재 모델은 진정한 generalist가 아니라 학습 데이터에 표현된 환경에 특화된 것이라고 진술한다.

**VLA-Arena (2512.22539).** L0~L2 난이도 축과 언어(W0-W4)·시각(V0-V4) 섭동 진단 축을 분리해 평가한다. 자기회귀·확산 계열 SOTA VLA들에 대한 공통 결론은 네 가지다. 일반화보다 암기, 표면적 시각 인식, 안전 제약 무시, 장기 스킬 조합 실패. 특히 L0에서 L1/L2 조합 과제로 갈 때 모든 모델이 급격한 성공률 저하를 보였고, L0~L2 사이에서 모델 순위 역전이 발생해 각 레벨이 서로 다른 정보를 준다는 것을 확인했다.

정리하면, 2026년 시점의 평가 인프라는 "실기 크라우드소싱(RoboArena)", "real-to-sim 확장(RobotArena∞)", "축 분리 진단(VLA-Arena)"의 세 갈래로 분화했고, 세 갈래 모두 현행 VLA의 일반화 주장에 부정적인 실측을 내놓고 있다.

---

## 7. 미해결 도메인

각 항목은 그렇게 판단한 근거 문헌을 함께 표기한다.

### 7.1 접촉·힘 정보의 부재 — 가장 넓은 공백

VLA는 이름 그대로 시각과 언어에 한정되어 있고, 삽입·조립·미끄러짐 감지처럼 카메라가 원리적으로 볼 수 없는 국소 상호작용을 요구하는 과제에서 구조적으로 실패한다(2511.05936 §2.1, 2606.11743, 2608.15816). 2026년에 이 공백을 메우려는 논문이 폭발적으로 늘었다는 사실 자체가 문제의 크기를 보여준다. TaF-VLA(2601.20321, 촉각-힘 정렬, 7개 힘 중요 과제에서 SOTA 시각-촉각 정렬 기준선 대비 평균 22% 성공률 개선), ForceVLA / ForceVLA2(2603.15169, 힘 인식 MoE와 힘-위치 하이브리드 제어), ViTaR(2608.15816, OpenVLA-OFT 동결 기준 대비 30.6pp 개선, UniVTAC 7개 과제 평균 61.3%), TacVLA(2603.12665), AT-VLA(2605.07308), CRAFT(2602.12532, 변분 정보 병목으로 힘 모달리티 우선 커리큘럼), FTP-1(2606.13102, 센서 간 일반 촉각 정책), TacForeSight(2606.11184, 손목 힘/토크로 촉각 잠재 예측을 조건화), OmniVTA(2603.19201), DreamTacVLA(2512.23864), AnyTouch 2(2602.09617).

**남아 있는 문제.** 이 논문들은 대부분 특정 센서·특정 과제군에 묶여 있고, 촉각 센서 하드웨어의 이질성이 표준화되지 않았으며(FTP-1이 센서 간 일반화를 명시적 목표로 삼는 이유), 대규모 사전학습 코퍼스에 촉각·힘 채널이 없다는 근본 문제가 남는다. 그리고 Gemini Robotics 2 자신의 평가처럼, 다지 손 정밀 조작은 촉각을 붙여도 여전히 낮은 성공률 구간이다.

### 7.2 장기 과제와 조합 일반화

VLA-Arena의 L1/L2 실측이 직접 근거다. 학습된 스킬을 조합해 장기 과제를 구성하지 못한다. 변형체 서베이(2312.10419) 역시 서브스킬의 조합 일반화를 장기 과제의 핵심 난제로 지목하며, 다지 손이나 다중 도구를 필요로 하는 장기 과제는 거의 손대지 못한 영역이라고 평가한다. 도구 사용은 별도로 어려운데, 커피를 만들려면 컵과 스푼을 식별하고 사용해야 하는 식의 일상적 요구조차 VLA가 다루지 못한다는 것이 open challenges 논문의 진단이다(§2.2).

### 7.3 Cross-embodiment / 액션 이질성

액션 공간이 다른 embodiment로의 전이가 안 된다. DoF가 다르거나 로봇팔·사족·차량처럼 구조가 다르면 고정된 embodiment 집합으로 학습한 모델이 실패한다(Zheng et al. 2025, 2511.05936 §2.5). 2026년 들어 π0.7이 zero-shot cross-embodiment를, DreamZero가 30분 play 데이터 기반 few-shot 적응을, Gemini Robotics On-Device 2가 수 시간 데이터로의 빠른 적응을 각각 주장하면서 상황이 움직이고 있으나, 이들 주장은 대부분 자체 평가이고 독립 검증이 없다. UniAct 저자들 스스로 진정한 zero-shot에는 미달한다고 기술한다.

### 7.4 평가의 신뢰성

6장 전체가 근거다. 시뮬레이션-실기 상관 부족, 표준화와 다양성의 상충, 그리고 "암기 대 일반화"를 구분할 진단 도구의 부재. 이 문제는 다른 모든 미해결 항목의 판정을 어렵게 만든다는 점에서 메타적 위치에 있다.

### 7.5 온보드 자원 제약

로봇은 공간·전력 제약으로 학습 인프라보다 훨씬 적은 연산만 가진다. 씬 클라이언트로 서버에 추론을 위임하는 구조는 네트워크 지연과 단절에 취약하고, 온보드용 소형 VLA는 대형 모델보다 성능이 떨어진다(2511.05936 §2.6). WAM 계열은 이 문제가 더 심각하다. DreamZero가 38배 최적화를 거쳐 도달한 것이 7Hz다.

### 7.6 접촉 모드의 전역 최적화

MPC 계열의 핵심 미해결 문제. 실시간에 접촉 시퀀스 공간을 전역 최적화하는 것은 불가능하고, 국소 근사는 근방 접촉만 활용한다(2505.13350 서두). 샘플링과 국소 상보성의 결합, 계층적 RL-MPC 등이 부분해를 내놓았지만 어느 것도 문제를 해소했다고 주장하지 않는다. 실기 배치 논문(2606.20712)이 추가로 지적한 접촉 민감성과 실시간 도메인 랜덤화 신호 부족은 여기에 얽혀 있다.

### 7.7 변형체·유체·다물체

변형체 조작 서베이(2312.10419, Neurocomputing 2026 개정판)의 진단은 단기 과제에서는 상당한 진전이 있었으나 장기 과제는 열려 있다는 것이다. 도구-물체 상호작용 이해 부족으로 로봇 도구 사용이 제약되고, RoboCook 같은 다중 도구 장기 시스템도 반죽이 도구에 달라붙는 식의 실패가 빈번하다고 명시한다. 이 서베이의 향후 방향 제안은 VLA + RL + 월드모델의 알고리즘적 시너지, 촉각 센서를 갖춘 범용 고 DoF 다지 손, 대규모 종합 평가 세 가지다.

### 7.8 안전 보증과 형식 검증

SafeVLA(2503.03480, NeurIPS 2025 spotlight)가 CMDP·안전 RL로 위반 비용 83.58% 감소를 보고했고, ProbeAct가 CBF 필터를 사후에 얹는 접근을 보였지만, 두 방향 모두 통계적 개선이지 보증이 아니다. VLA-Arena는 현행 모델들이 안전 제약을 아예 고려하지 않는다는 것을 실측했다. 물리적 행동은 직접 해를 끼칠 수 있다는 점에서 LLM 안전 문제와 성격이 다르며(2511.05936 §2.8), 학습 정책에 대한 형식 검증은 사실상 백지 상태다.

### 7.9 실패 감지·복구·자율 개선

π*0.6이 정면으로 다루는 문제이지만, 그 논문 자신이 남긴 미해결 목록(대규모 모델 RL의 안정성, 표본 복잡도, 자율성, 지연 피드백)이 곧 이 영역의 현주소다. CycleVLA(서브태스크 백트래킹), ProbeAct(실패 감지 상태 기계), SC-VLA(느린 교정 경로) 등이 부분적으로 접근한다. 온로봇 RL이 실용 도구가 되려면 추가 연구가 필요하다는 것이 Physical Intelligence의 공식 입장이다.

### 7.10 전신 협응

이동과 조작을 불확실성 하에서 결합하는 문제. MPC는 정확한 모델을 요구하고, 차원과 비평활 접촉 이벤트가 늘면 실시간 병목에 걸리며, embodiment별 튜닝이 필요해 상위 비전-언어 계획과의 통합이 어렵다. 학습 정책은 탐색과 신용 할당에 어려움을 겪고 보상 설계에 민감하며 embodiment 간 일반화가 나쁘다. open challenges 논문은 해석적 구조와 학습 유연성을 결합하는 하이브리드 제어를 유력한 경로로 제시하지만(§2.7), 이는 방향 제시이지 해결이 아니다. Gemini Robotics 2가 전신 제어를 실증했으나 다지 정밀 조작은 여전히 미해결이라고 스스로 밝힌 것과 일치한다.

### 7.11 sim-to-real과 인간 영상 전이의 메커니즘

로코모션에서는 시뮬레이션 격차가 좁혀졌지만 매니퓰레이션에서는 심각하게 남아 있다는 것이 업계 정리의 공통 진술이다. 인간 영상 전이는 스케일링 법칙이 확인되었으나(EgoScale, HumanScale), DreamZero 논문 스스로 "인간 영상이 액션 주석 없이 다양한 스킬을 습득하게 할 잠재적 스케일링 경로를 열지만 전이 메커니즘 강화에 대한 추가 연구가 필요하다"고 유보를 단다. 즉 "된다"는 확인되었고 "왜, 어디까지 되는가"는 미해결이다.

### 7.12 학습 정책과 최적화 계층의 인터페이스

*이 항목은 개별 논문이 단일 문제로 명명한 것이 아니라, 여러 문헌의 한계 진술을 교차해 정리자가 묶은 것이므로 다른 항목과 근거 성격이 다르다.*

VLA는 제약을 만족한다는 보증 없이 액션 청크를 내놓고, cuRoboV2·CI-MPC는 명시적 제약을 만족시키지만 무엇을 해야 하는지를 모른다. 현재의 결합 방식은 대체로 사후 필터(CBF), 계층 분해(상위 RL이 접촉점 선택, 하위 MPC가 실행), 또는 학습된 사전으로 최적화 초기해 제공(MPD, DiffusionSeeder) 세 가지다. 어느 것도 두 계층의 목적함수를 일관되게 정의하지 않으며, 학습 정책의 다봉 분포와 최적화의 국소 수렴을 어떻게 정합시킬지에 대한 표준이 없다. open challenges 논문이 하이브리드 제어 프레임워크를 "미래 진전이 걸려 있는 지점"이라고 표현한 것이 이 상황의 간접 근거다.

---

## 8. 계열 간 상호 보완의 실제 상태

객관적으로 확인 가능한 사실만 정리하면 다음과 같다.

VLA는 의미 일반화(새 지시, 새 물체 이름)에서 강하고 물리 동작 일반화에서 약하다. 이는 DreamZero가 WAM을 제안하며 든 문제 설정이자, RobotArena∞와 VLA-Arena가 실측한 내용이다.

WAM은 물리 동작 일반화를 개선하지만 연산 비용이 한 자릿수 규모로 늘고 제어 주파수가 낮다. NVIDIA 블로그가 명시한 ZFLOPs 비교와 DreamZero의 7Hz가 근거다.

궤적 최적화는 실행 가능성과 안전을 밀리초 단위로 보장하지만 무엇을 해야 하는지에 대한 의미론이 없고, 접촉이 개입하면 문제 자체가 조합적으로 폭발한다.

MPC는 접촉과 외란에 반응하지만 정확한 모델을 요구하고, 접촉 모드의 전역 탐색이 실시간에 불가능하다.

따라서 2026년 현재 각 계열의 약점은 다른 계열의 강점과 거의 정확히 상보적이다. 그럼에도 통합 프레임워크가 표준화되지 않은 이유는 인터페이스 정의(7.12)와 평가 방법(7.4)이 함께 미해결이기 때문으로 보인다. 이 마지막 인과 진술은 문헌이 직접 주장한 것이 아니라 정리자의 해석이다.

---

## 9. 자료 링크

**논문 목록·서베이**
- Awesome-Robotics-Manipulation: https://github.com/BaiShuanghao/Awesome-Robotics-Manipulation
- Awesome VLA: https://github.com/KwanWaiPang/Awesome-VLA
- awesome-embodied-vla-va-vln (2026 논문 추적이 빠름): https://github.com/jonyzhang2023/awesome-embodied-vla-va-vln
- Awesome-Robotics-Diffusion: https://github.com/showlab/Awesome-Robotics-Diffusion
- Towards a Unified Understanding of Robot Manipulation (2510.10903)
- 10 Open Challenges Steering the Future of VLA Models (2511.05936, AAAI 2026)
- 변형체 조작 서베이 (2312.10419)

**코드·모델**
- openpi (π 계열): https://github.com/Physical-Intelligence/openpi
- DreamZero: https://github.com/dreamzero0/dreamzero
- cuRobo / cuRoboV2: https://github.com/nvlabs/curobo
- RoboArena: https://github.com/robo-arena/roboarena
- AgiBot World: https://github.com/OpenDriveLab/AgiBot-World

**공식 릴리스 페이지**
- π0.7: https://www.pi.website/blog/pi07
- π*0.6: https://pi.website/blog/pistar06
- Gemini Robotics 2: https://deepmind.google/blog/gemini-robotics-2-brings-whole-body-intelligence-to-robots/
- Isaac GR00T: https://developer.nvidia.com/isaac/gr00t
